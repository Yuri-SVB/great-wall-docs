% ════════════════════════════════════════════════════════════════════════════
\section{Ethics and responsible disclosure}
\label{sec:ethics}
% ════════════════════════════════════════════════════════════════════════════

This is offensive-flavored security research with direct life-safety stakes: we
formally derive an \emph{assassination} incentive (Lemma~\ref{lem:drl}). We
therefore state plainly why publishing it is, on balance, protective.

\textbf{The incentive is disclosed, not created.} The Deadly Race is a property of a
situation a coercer already faces; naming it hands nothing to an attacker who, by
Kerckhoffs (Assumption~\ref{ass:kerckhoffs}), is already assumed to know the protocol
and the landscape. What publication changes is only that \emph{defenders} can now
measure a design against the bar (Criterion~\ref{prin:nga}) and reject the ones that
manufacture the incentive. We describe an incentive and the failure modes of existing
defenses --- not a targeting method, and no operational how-to; the paper's
constructive content is defensive.

\textbf{Obscurity is not a sound reason to withhold.} The alternative --- not
publishing analyses of attack vectors and the failure modes of deployed defenses ---
is itself a bet on obscurity, and both theory and data say that bet loses.
As Kerckhoffs' principle has held for a century, a defense that works only while the
adversary stays ignorant provides something worse than defenselessness: \emph{a false sense 
of security}. Empirically, the physical-attack registry~\cite{lopp-attacks} and the 
accompanying economic analysis~\cite{gw-justification} show exactly what the principle 
predicts --- wrench attacks are frequent and rising, and the community's entrenched counsel
(\emph{keep a low profile}, \emph{never disclose ownership}, \emph{deny if asked} ---
advice prominent channels actively promote, cataloged in~\cite{gw-justification})
has not stemmed them. Security knowledge diffuses to criminal actors regardless of
academic reticence; withholding it handicaps only the defenders who would otherwise
design against the very incentive we name. Publishing the Deadly Race, \emph{together
with} a bar that rejects the designs which create it, is net-protective of the
holders it concerns.

% Incident facts verified against primary sources (2026-08-03): defect shipped
% in firmware v4.0.0 (2021-03-17) --- an #ifndef guard accepted a zero-valued
% macro, routing seed generation to the software fallback PRNG (Block/Coinkite
% post-mortems); dice-cohort claim per the vendor advisory; fixed firmware for
% all affected models by 2026-08-01; first-wave sweep figures per CoinDesk
% (Galaxy Research mapping).
\textbf{The quiet years compound --- a supply-side precedent.} The ecosystem has
already run this experiment on itself, on the \emph{generation} side of the key
lifecycle. A March-2021 firmware defect left a fallback software PRNG silently
live as the production entropy path of a major hardware wallet
(Coldcard)~\cite{block-coldcard-postmortem,coinkite-entropy}; the weakness was
invisible to inspection --- pseudorandom output passes every eyeball test, which
is why entropy health cannot be observed from outside --- and it stayed quiet
for over five years, until a first wave of sweeps drained ${\approx}\$70$M from
${\approx}1{,}200$ addresses in 41 minutes on 30 July
2026~\cite{coindesk-coldcard}. The episode teaches two
things, and both transfer to coercion verbatim. First, the quiet years were not
obscurity \emph{working}: the loss was contracted the moment the defect shipped,
and every silent month enrolled more holders into the eventual sweep ---
exposure compounding while it read, to everyone, as safety. Second, there was no
``quiet fix'' even in principle: a warning channel wide enough to reach the
holders is wide enough to reach the attacker, so disclosure and attack start the
same clock, and the only tenable posture is a design that survives full
disclosure (the vendor's prompt patch --- fixed firmware for every affected
model within two days --- and candid post-mortem being the honest version of
exactly that). Wrench-resistance sits in the same epistemic position
as entropy health: a holder cannot observe from outside whether their custody
design protects them or endangers them. The community's collective silence about
deadly-race dangers --- the norm against saying plainly that
feasible-but-slower recovery paths endanger their owners --- compounds exposure
the same way the quiet years did, while it, too, reads as prudence. Naming the
incentive is the disclosure; the quiet alternative is how it gets worse.

\textbf{Framing and restraint.} We keep the framing on protecting at-risk holders,
disclose our own bounded residual honestly (\S\ref{sec:residual-seizure}) rather than
overclaim, and treat the shipping alternatives we classify (\S\ref{sec:classify}) as
legitimate clearances by the other route --- not targets.
